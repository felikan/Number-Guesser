/*var player_win = 0
var enemy_win = 0
var diff_player = 0
var diff_enemy = 0
var i = 0
var trys = 0


while(i == 0){
    trys = trys +1
    var generate = Math.floor(Math.random() * 10);
    var enemyNumber = Math.floor(Math.random() * 10);
    var playerNumber = Math.floor(Math.random() * 10);

    diff_enemy = generate -enemyNumber
    diff_player = generate -playerNumber

    if(diff_player < diff_enemy){
        enemy_win = enemy_win +1
    }
    if(diff_player > diff_enemy){
        player_win = player_win +1
    }
    console.log(player_win, enemy_win, player_win/trys, enemy_win/trys, "Versuche:",trys)
}

*/